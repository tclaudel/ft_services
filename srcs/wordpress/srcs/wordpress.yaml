apiVersion: v1
kind: Service
metadata:
  name: wordpress
  labels:
    app: ft_services
spec:
  type: LoadBalancer
  selector:
    app: ft_services
    tier: wordpress
  ports:
    - name: http
      port: 5050
      targetPort: 5050
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wordpress
  labels:
    app: ft_services
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ft_services
      tier: wordpress
  template:
    metadata:
      labels:
        app: ft_services
        tier: wordpress
    spec:
      volumes:
        - name: wordpress-pv-storage
          persistentVolumeClaim:
            claimName: mysql-pvc
      containers:
        - name: wordpress
          image: ft_wordpress
          imagePullPolicy: Never
          volumeMounts:
            - name: wordpress-pv-storage
              mountPath: "/var/lib/mysql"
