apiVersion: v1
kind: ConfigMap
metadata:
  namespace: metallb-system
  name: config-metallb
data:
  config: |
    address-pools:
      - name: my-ip-space
        protocol: layer2
        addresses:
          - IPADDRESSES